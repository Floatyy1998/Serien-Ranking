/** Englische Übersetzungen: Aktivitäts-Ticker (MobileApp). */

const dict: Record<string, string> = {
  '{name} hat "{title}" hinzugefügt': '{name} added "{title}"',
  '{name} hat "{title}" auf die Watchlist gesetzt': '{name} added "{title}" to their watchlist',
  '{name} hat "{title}" mit {rating}/10 bewertet': '{name} rated "{title}" {rating}/10',
  '{name} möchte dein Freund werden': '{name} wants to be your friend',
};

export default dict;
