/**
 * Feature Flags - Schnelles Ein-/Ausschalten von Features
 */

export const FEATURES = {
  // Wrapped Jahresrückblick aktivieren/deaktivieren
  WRAPPED_ENABLED: false,

  // Jahr für Wrapped (wird jedes Jahr geändert)
  WRAPPED_YEAR: 2026,
};

export default FEATURES;
